# image loading example

## build

```
cmake -B build
cmake --build build
```

## try the executable

* run the application in [this](./) folder (it will use `blume2.png`).
![image](./blume2.png)

* or pass as commandline argument some image: `./build/main blume2.png`
